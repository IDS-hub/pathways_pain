# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.
# todo: make it encrypted; as ENV?
shared:
  admin_email: "admin@pathways.com"
  admin_password: "qAz1wSx2"

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 6d630c2298bdf5412ba087b8c68f0fead3723afcc777da9aab829d414db34ed62bea86e56ce7468b243defa8b0e71206df9864e4cbf79fa15e277b5f210fa522

test:
  secret_key_base: 9d225d44f4236df11a7186e405497067e2005895356e8bbad8b40794717c3a077003785e239fee38d180625a1155fa99df7103b6b6e1b0c99235567eb0374891

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
